[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "monorepo"
version = "0.1.0"

[tool.setuptools.packages.find]
# pyproject.tomlから見て、src/ がソースコードのルート
where = ["src"] # srcディレクトリの中をパッケージとして探す